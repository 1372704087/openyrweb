/**
 * CdnManifest — CDN 资源清单（manifest.json）模块。
 *
 * 孪生 engine/gameRes/CdnManifest.ts.js 为“空模块”：System.register
 * 无任何导出，仅占位（类型/接口可能在编译时被擦除）。此处忠实还原为
 * 无运行时导出的空 TS 模块；若需清单结构类型，应在使用方本地声明。
 *
 * 由 engine/gameRes/CdnManifest.ts.js 逆向翻译为 TS（行为完全一致）。
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先
 * 采用 .ts 模块的编译产物。
 */
export {};
