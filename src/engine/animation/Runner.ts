/**
 * Runner — 动画驱动器接口（空模块占位）。
 *
 * 由 engine/animation/Runner.ts.js 重写为 TS（行为完全一致）。
 * 原 SystemJS 模块 execute 为空、无导出；此处仅保留模块文件以对齐打包路径。
 *
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先
 * 采用 .ts 模块的编译产物。
 */
export {};
