/**
 * PlayerUi — 玩家侧 UI 占位模块（type-only 空模块）。
 *
 * 由 gui/screen/game/PlayerUi.ts.js 重写为 TS。
 * 孪生 System.register 的 execute 为空、无运行时导出；此处同样只保留
 * 空模块语义，类型可能在别处以结构类型使用。
 *
 * 由 gui/screen/game/PlayerUi.ts.js 重写为 TS（行为完全一致）。
 *
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 */
export {};
