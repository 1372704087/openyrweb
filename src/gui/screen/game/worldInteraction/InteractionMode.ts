/**
 * InteractionMode — 世界交互模式标记类型（type-only 空模块）。
 *
 * 由 gui/screen/game/worldInteraction/InteractionMode.ts.js 重写为 TS。
 * 孪生 execute 为空、无运行时导出；此处仅保留空模块语义。
 *
 * 由 gui/screen/game/worldInteraction/InteractionMode.ts.js 重写为 TS
 * （行为完全一致）。
 */
export {};
