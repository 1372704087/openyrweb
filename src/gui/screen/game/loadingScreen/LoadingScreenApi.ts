/**
 * LoadingScreenApi — 加载屏对外 API 接口（type-only 空模块）。
 *
 * 由 gui/screen/game/loadingScreen/LoadingScreenApi.ts.js 重写为 TS。
 * 孪生 execute 为空、无运行时导出；此处仅保留空模块语义，
 * 具体 API 形状由 Sp/Mp/Replay 实现类体现。
 *
 * 由 gui/screen/game/loadingScreen/LoadingScreenApi.ts.js 重写为 TS
 * （行为完全一致）。
 *
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 */
export {};
