/**
 * Voxel — 体素相关类型占位模块。
 *
 * 孪生 data/vxl/Voxel.ts.js 为空 execute（无导出）；此处保持一致：
 * 不声明任何导出，避免打包差异。
 *
 * 由 data/vxl/Voxel.ts.js 重写为 TS（行为完全一致）。两个文件并存期间，
 * 本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 */
export {};
