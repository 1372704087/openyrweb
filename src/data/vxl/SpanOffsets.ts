/**
 * SpanOffsets — VXL span 偏移表占位模块。
 *
 * 由 data/vxl/SpanOffsets.ts.js 重写为 TS（行为完全一致）。两个文件并存期间，
 * 本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 *
 * 孪生 execute 为空：不导出任何符号；本文件仅为保持模块路径可解析。
 */
export {};
