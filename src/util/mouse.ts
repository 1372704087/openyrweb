/**
 * mouse — 鼠标事件工具空标记模块。
 *
 * 孪生 SystemJS 模块无任何导出（仅注册空 execute），此处以空导出模块
 * 保持“可 import 且无副作用”的等价行为。
 * 由 util/mouse.ts.js 重写为 TS（行为完全一致）。两个文件并存期间，
 * 本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 */

export {};
