/**
 * Locomotor — 移动器基类（空模块）。
 *
 * 原始工程中此处为空导出（移动器基类逻辑由各子类自行实现）。
 * 由 game/gameobject/locomotor/Locomotor.ts.js 重写为 TS（行为完全一致）。
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先
 * 采用 .ts 模块的编译产物。
 */
export {};
