/**
 * WeaponInfo — 占位模块。
 *
 * 原始工程中此处为空导出（武器运行时信息由 Weapon 类承载，本模块仅
 * 维持模块图完整）。
 *
 * 由 game/WeaponInfo.ts.js 重写为 TS（行为完全一致）。
 *
 * 两个文件并存期间，本文件才是修改目标：tools/repack.mjs 打包时优先采用 .ts 模块的编译产物。
 */
export {};
