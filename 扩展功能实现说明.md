# OpenYRWeb 扩展功能实现说明

本文档记录引擎中已实现的机制及其**实现位置**，供未来实现 Ares / Phobos / MO 等扩展功能时参考。
源码均为 SystemJS 重构的混淆代码（`src/**/*.ts.js`），修改时注意保持模块依赖列表与代码风格一致。

---

## 一、驻军武器系统（OccupyWeapon / EliteOccupyWeapon）

### 原版语义
- 步兵 `Occupier=yes` 进驻建筑后，使用独立的驻军武器开火（GI 用 `UCPara`，精英用 `UCElitePara`），**不是**主武器。
- `[CombatDamage]` 三项加成：
  - `OccupyWeaponRange=5`：覆盖驻军武器射程（固定值，混编不同射程士兵时统一判定，避免短射程者拖累建筑停火——原版注释原话）。
  - `OccupyDamageMultiplier=1.2`：伤害 ×1.2。
  - `OccupyROFMultiplier=1.2`：ROF ÷1.2（值越大射速越快）。
- 该三项与 OpenTopped（运输车乘客）**互相独立**。

### 实现位置
| 功能 | 文件 | 说明 |
|---|---|---|
| `occupyWeapon` 创建 | `src/game/gameobject/trait/ArmedTrait.ts.js` `selectStandardWeapons()` | 按精英状态取 `EliteOccupyWeapon`/`OccupyWeapon`，未配置回退主武器 |
| `getGarrisonWeapon()` | 同上 | `occupyWeapon \|\| primaryWeapon` |
| 驻军武器冷却递减 | `ArmedTrait.onTick` | 建筑侧驱动，遍历 `garrisonTrait.units` 递减 |
| 驻军武器列表 | `src/game/gameobject/trait/AttackTrait.ts.js` `getAvailableWeapons()` / `selectDefaultWeapon()` | 建筑可瞄准仅驻军武器能打的目标 |
| 独立开火 | `src/game/gameobject/task/AttackTask.ts.js` Firing 段 | 每个士兵各自查冷却 + canTarget 后 `fire()`，互不影响 |
| 反向引用 | `src/game/gameobject/task/GarrisonBuildingTask.ts.js` `onEnter` | `e.garrisonedAt = 建筑`；`GarrisonTrait.evacuate`/`onDestroy`、`EvacuateBioReactorTask` 清除 |
| 射程/伤害/射速加成 | `src/game/Weapon.ts.js` `get range` / `get rof` / `fire()` | 通过 `gameObject.garrisonedAt` 判定驻军状态 |
| 配置解析 | `src/game/rules/CombatDamageRules.ts.js`；传播到 `Weapon` 静态值在 `src/game/rules/Rules.ts.js` `readCombatDamage()` | 默认 5 / 1.2 / 1.2 |
| 被动开火豁免 | `AttackTrait.shouldPassiveAcquire()` / `shouldRetaliate()` | 建筑无自身武器但驻军时放行 |
| 守卫扫描半径 | `ArmedTrait.computeGuardScanRange()` | 驻军建筑取驻军武器射程 |
| 开火位置 | `Weapon.fire()` / `getMuzzleFacing()` | 驻军士兵 limbo，弹道以建筑为 fireOrigin |

### 注意事项
- 生物反应炉（`InfantryAbsorb=yes` + `bioReactorPowerTrait`）不挂 `ArmedTrait`/`AttackTrait`，吸收的步兵不会开火。
- G 键守卫命令对驻军建筑已放行（`src/game/order/GuardAreaOrder.ts.js` `isValid()`），建筑不创建 MoveTask。

---

## 二、OpenTopped 乘客开火（战斗要塞 BFRT）

### 原版语义
- 车内乘客（最多 5 名）从 `AlternateFLH0-4` 炮口独立开火，享受 `OpenToppedRangeBonus=2`（射程+2）、`OpenToppedDamageMultiplier=1.2`（伤害×1.2）。
- `OpenTransportWeapon`：0=主武器，1=副武器，-1=默认（主武器）。GGI 用 1 在车内打导弹。
- 乘客在车辆移动中也可开火。

### 实现位置
| 功能 | 文件 |
|---|---|
| `getOpenToppedWeapon()` | `ArmedTrait.ts.js` |
| 乘客武器列表/被动扫描 | `AttackTrait.ts.js` `getAvailableWeapons()` / `scanForTarget()` |
| 乘客独立开火 / 移动中机会射击 | `AttackTask.ts.js` |
| 反向引用 `passenger.transport` | `EnterTransportTask.ts.js`；`EvacuateTransportTask.ts.js` / `TransportTrait.ts.js` 清除 |
| 炮口位置 `getAlternateFlh(index)` | `src/game/art/ObjectArt.ts.js`（解析 `AlternateFLH{index}`） |
| 射程/伤害加成 | `Weapon.ts.js`（`gameObject.transport` 判定） |
| 加成配置 | `CombatDamageRules.ts.js` + `Rules.ts.js` |

---

## 三、未来实现 Ares/Phobos 类功能的扩展点

1. **驻军射程按士兵计算（类似 MO3.3.6）**
   - 当前：`Weapon.get range` 中 `garrisonedAt` 时 `baseRange = S.occupyWeaponRange`（对齐原版固定 5）。
   - 若改：去掉该覆盖，并在 `AttackTask` Firing 段给每个士兵补独立 `isInWeaponRange` 检查（参考 OpenTopped 的 CheckRange 乘客块写法）。架构上驻军士兵本就是独立开火，不会出现原版"短射程者拖累"问题。

2. **按槽位/建筑自定义炮口 FLH**
   - 建筑开火位置当前用士兵自身 FLH（相对建筑中心）。若需按建筑定义（类似 NABNKR 的 `MuzzleFlash0-4`），参考 `ObjectArt.getAlternateFlh()` + `Weapon.fire` 的 OpenTopped 分支模式扩展。

3. **新增 `[CombatDamage]` 类全局参数**
   - 三步走：`CombatDamageRules.readIni()` 解析 → `Rules.readCombatDamage()` 传播到 `Weapon`/模块静态值 → 在使用处读取。参考 `OpenToppedRangeBonus` / `OccupyWeaponRange` 的现有实现。

4. **新 trait / 新任务**
   - 挂载条件在 `src/game/gameobject/ObjectFactory.ts.js`（如 `AttackTrait` 的 `primaryWeapon || secondaryWeapon || garrisonTrait` 判断）；任务状态机参考 `AttackTask.ts.js`（CheckRange → PrepareToFire → Firing → JustFired）。
