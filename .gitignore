# Dependencies
node_modules/

# Build output (regenerated by `npm run build`)
build/

# Original upstream product / vendor-src baseline (fetched on demand only;
# never distributed, never part of this repo's source)
client/
vendor-src/

# Game data — user-supplied, copyrighted by EA. NEVER distributed/committed.
assets/
*.mix
*.csf.bak

# Tooling scratch
.fetch-manifest.json
.tmp/

# Reverse-engineering workspace (internal; not for the public repo)
re/
docs/

# Internal agent / session notes (not for public repo)
AGENTS.md
.zcode/
.zcode-final-base.png
.pi/

# Playwright browser cache
.browsers/

# OS/editor cruft
.DS_Store
*.swp
